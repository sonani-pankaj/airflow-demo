# Airflow Production Environment Configuration
# Copy this file to .env and fill in the values
# NEVER commit the .env file to version control!

# =============================================================================
# REQUIRED - Must be set for production
# =============================================================================

# Airflow user ID (run 'id -u' on Linux/Mac to get your user ID)
AIRFLOW_UID=50000

# PostgreSQL passwords - USE STRONG PASSWORDS IN PRODUCTION
POSTGRES_PASSWORD=change_me_to_strong_password
POSTGRES_EXTERNAL_PASSWORD=change_me_to_strong_password

# Fernet key for encrypting connections/variables
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=

# Secret key for webserver sessions
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
AIRFLOW_SECRET_KEY=

# Admin username for Airflow UI (used with SimpleAuthManager)
# Password is set on first login via the UI
AIRFLOW_WWW_USER_USERNAME=admin

# =============================================================================
# OPTIONAL - Defaults provided
# =============================================================================

# Project directory (default: current directory)
# AIRFLOW_PROJ_DIR=.

# Custom environment file path
# ENV_FILE_PATH=.env
